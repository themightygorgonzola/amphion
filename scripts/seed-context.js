/**
 * seed-context.js — Populate the user_context table in SQLite
 *
 * Run this once to set up the living context profile for the boss.
 * Re-run anytime to update priorities, active deals, key contacts, etc.
 *
 * Usage:
 *   node scripts/seed-context.js
 *
 * TODO (next session):
 *   - [ ] Connect to SQLite via better-sqlite3
 *   - [ ] Upsert a single row into user_context
 *   - [ ] Fields: name, company, role, current_priorities, active_deals,
 *               key_contacts, tone_preferences, updated_at
 */

console.log('[seed-context] stub — not yet implemented')
